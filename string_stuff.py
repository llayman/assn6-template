def reverse_words(sentence: str) -> str:
    """Reverse the words in a string. Words are separated by one or more spaces.

    :param sentence: The string to reverse.
    :raises ValueError: If the sentence is empty.
    :raises TypeError: If the sentence is not a string.
    :return: the sentence with the words reversed reversed string.
    """
    raise NotImplementedError("reverse_words() not implemented")  # Delete this line to write the function

VOWELS = "aeiouAEIOU"
def count_vowels(text: str) -> int:
    """Count the number of vowels in a string.

    :param text: the string to count vowels in
    :raises TypeError: raised if text is not a string
    :raises ValueError: raised if text is empty
    :return: the number of vowels in the string
    """
    raise NotImplementedError("count_vowels() not implemented")  # Delete this line to write the function

def is_palindrome(text: str) -> bool:
    """Check if a string is a palindrome.

    :param text: the string to check
    :raises TypeError: raised if text is not a string
    :raises ValueError: raised if text is empty
    :return: True if the string is a palindrome, False otherwise
    """
    raise NotImplementedError("is_palindrome() not implemented")  # Delete this line to write the function
